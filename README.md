# Соснина Надежда, КМБ - 2 Лабораторная №1

# Задание 1. ООП

## Задача 1

### Текст задачи

Разработать класс с тремя вещественными полями. Создать конструктор копирования.
Разработать метод, приводящий поля к целому типу. Перегрузить метод ToString() для
формирования строки из полей класса. Реализовать дочерний класс (его содержание предложить
самостоятельно и описать в решении: какой содержательный смысл имеют поля; реализовать
конструкторы; предложить и реализовать 2-3 метода). Протестировать все конструкторы и
другие методы базового и дочернего классов.

### Алгоритм решения

1) Запрашиваем у пользователя три вещественных числа - длину, ширину и высоту параллелепипеда.
2) Методом класса проверяем на корректность ввода.
3) Создаём объкт дочернего класса Parallelepiped, в который передаются эти три числа.
4) Значения сохраняются внутри объекта (в базовом классе).
5) Когда нужно получить значения — они берутся через специальные свойства (Number1, Number2, Number3).
6) Для вычисления методом класса вычисляем:
	-объём: длина × ширина × высота
	-площадь поверхности: считается сумма площадей всех сторон по формуле, результат умножается на 2
7) При выводе объекта перегрузка метода ToString.
8) Для копия объекта: берутся те же значения и записываются в новый объект.
9) Для преобразований числа в целые метод: дробная часть отбрасывается.


### Тестирование

<img width="437" height="141" alt="Test1" src="https://github.com/user-attachments/assets/5862f96b-b45a-444d-bcc5-8ac70419e76e" />


# Задание 2. Перегрузка операций

## Задача 1

### Текст задачи

Вычислить расстояние от точки до начала координат. Результат должен быть типа double.

### Алгоритм решения

1) Запрашиваем с пользователя координаты точки, методом класса Point проверяем на корректность ввода.
2) Создаём объект класса Point:
	-введённые значения сохраняются в _x и _y
3) Вычисляем расстояние до начала координат методом класса Point:
	-используется формула √(x² + y²)
4) Выводим результат при помощи перегрузки метода ToString 

## Задача 2

### Текст задачи

Унарные операции:
--уменьшить координаты xи yна 1;
- поменять координаты х и у местами.
Операции приведения типа:
int (неявная) – результатом является целая часть координаты х;
double(явная) – результатом является координата y.
Бинарные операции:
– Point p, целое число (левосторонняя операция, уменьшается координата
х);
– целое число, Point p (правосторонняя операция, уменьшается
координата y);
– Point p – вычисляется расстояние до точки p, результатом должно быть
вещественное число

### Алгоритм решения

1) При помощи операций перегрузки создаём в классе методы увеличения координаты х на 1 и уменьшения на 1, выводим результат..
2) выводим результаты методов явного и неявного преобразования.
3) Запрашиваем с пользователя координаты второй точки, создаём второй объект класса Point.
4) Методом класса вычисляем расстояние междуь первым и вторым объектами, выводим.
5) Создём ещё два объекта класса Point при помощи метода лево- и правосторнних операций, выводим.
Унарные операции класса:
	++ увеличивает x на 1
	-- уменьшает x на 1
Метод класса приведение типов:
	-к int берётся только x (отбрасывается дробная часть)
	-к double возвращается y
Перегрузка операций с двумя точками:
	-p1 + p2 вычисляется расстояние между точками
	-можно прибавлять число к точке: Point + int или int + Point

### Тестирование

<img width="510" height="235" alt="Test23" src="https://github.com/user-attachments/assets/70013039-5420-42c9-8fab-c526d5008039" />

